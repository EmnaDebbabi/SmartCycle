{% extends "@User/Default/index.html.twig" %}
{% block slide %}
    <div class="inner_page-banner one-img">
    </div>
    <div class="using-border py-3">
        <div class="inner_breadcrumb  ml-4">
            <ul class="short_ls">
                <li>
                    <a href="{{ path('user_homepage') }}">Smart Cycle Accueil </a>
                    <span>/ /</span>
                </li>
                <li>Validation</li>
            </ul>
        </div>
    </div>

{% endblock %}



{% block body %}

    <div style="background-color: red">
        <p style="color: white">{{ msg2 }}</p>

    </div>
    <div style="background-color: red">
        <p style="color: white">{{ msg3 }}</p>

    </div>

    <div class="container py-lg-5 py-md-4 py-sm-4 py-3">
        <div class="shop_inner_inf">
            <div class="checkout-right-basket">
                <a href="{{ path('PasserauPaiement',{'idCmd':commande.idCmd })}}"> Payer Votre Commande </a>
            </div>
            <div class="privacy about">


                <a href="{{ path('sandbox_pdfexample',{'idCmd':commande.idCmd})}}"> Imprimer Votre Facture  </a>


            </div>


            <div class="clearfix"> </div>
            <div class="clearfix"></div>
               </div>
    </div>






{% endblock %}





{% block login %}
    <div>
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
            <a href="{{ path('fos_user_security_logout') }}">
                {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
            </a>

        {% endif %}
    </div>

    {% if app.request.hasPreviousSession %}
        {% for type, messages in app.session.flashbag.all() %}
            {% for message in messages %}
                <div class="flash-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    {% endif %}

    <div>
        {% block fos_user_content %}
        {% endblock fos_user_content %}
    </div>




{% endblock %}