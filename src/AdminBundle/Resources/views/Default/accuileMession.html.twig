{% extends "@Admin/Default/index.html.twig" %}
{% block body %}

    <center>

        <div class="table-responsive bs-example widget-shadow">
            <h1>Produit list</h1>
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th scope="col" hidden>Id produit</th>
                    <th>Nom </th>
                    <th>Type</th>
                    <th>Quantite</th>
                    <th>Description</th>
                    <th>Date</th>
                    <th colspan="2">Actions</th>
                </tr>
                </thead>
                <tbody>

                {% for c in produits %}
                    <tr>

                        <td hidden>{{ c.id }}</a></td>
                        <td>{{ c.nomproduit }}</a></td>
                        <td>{{ c.typeproduit.type }}</a></td>
                        <td>{{ c.quantiteproduit }}</a></td>
                        <td>{{ c.description }}</a></td>
                        <td>{{ c.dat |date('d-m-Y') }}</a></td>
                        <td>
                            <a href="{{ path('Messions_affectee', { 'id': c.id })  }}"><button class="btn btn-primary"> <i class="fa fa-edit" ></i> Affecte  </button></a>
                        </td>


                    </tr>






                {% endfor %}
                </tbody>
            </table>
        </div>




    </center>

{% endblock %}